[package]
name = "local/clases"
description = "SOTA class library for Luau and Roblox: fast direct-index classes, inheritance, super, lifecycle cleanup, strict tests, and Rojo packaging."
version = "0.1.0"
license = "MIT"
authors = ["ElWapoteDev"]
realm = "shared"
registry = "https://github.com/upliftgames/wally-index"
private = true
exclude = [
	"aftman.toml",
	"build",
	"benchmarks",
	"tests",
	".github",
	".lune",
	"docs",
	"dev.project.json",
	"examples",
	"pesde.toml",
	"selene.toml",
	"sourcemap.json",
	"stylua.toml",
]

[dependencies]
